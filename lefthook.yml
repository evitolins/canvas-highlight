pre-commit:
  parallel: false
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      run: npm run lint
    format:
      glob: 'src/**/*.{ts,tsx}'
      run: npm run format
      stage_fixed: true

pre-push:
  commands:
    test:
      run: npm run test:e2e
